# API Documentation

This directory contains OpenAPI specifications and endpoint documentation for UniSphere AI services.

* **Backend OpenAPI Specification**: Available dynamically via `http://localhost:8000/docs` when running the backend.
* **AI Service OpenAPI Specification**: Available dynamically via `http://localhost:8001/docs` when running the AI service.
